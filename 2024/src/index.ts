import { day1, day2, day3 } from "./days";

const getAnswers = async () => {
    // console.log('answers for day 1 are:', await day1());
    // console.log('answers for day 2 are:', await day2());
    console.log('answers for day 3 are:', await day3());
}

getAnswers();