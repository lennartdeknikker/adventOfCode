import getDataForDay from "../utils/getDataForDay";

export async function day19(): Promise<string> {
    const dataString = await getDataForDay(19);
    const answerA = 'answer A'
    const answerB = 'answer B'

    return `${answerA} ${answerB}`
}
